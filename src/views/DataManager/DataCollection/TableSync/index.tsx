import * as React from 'react';

export interface ITableSyncProps {
}

export default class TableSync extends React.Component<ITableSyncProps> {
  public render() {
    return (
      <div>
        库表同步
      </div>
    );
  }
}
